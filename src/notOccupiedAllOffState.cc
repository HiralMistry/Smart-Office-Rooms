#include <iostream>
#include <chrono>

#include "stopwatch.h"

using namespace std::chrono;
using namespace elma;
using namespace stopwatch;

void Not_Occupied_All_OFF::exit(const Event& e) {
    // No action needed.
}