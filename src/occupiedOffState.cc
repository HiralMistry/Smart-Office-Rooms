#include <iostream>
#include <chrono>

#include "stopwatch.h"

using namespace std::chrono;
using namespace elma;
using namespace stopwatch;

void Occupied_OFF::exit(const Event& e) {
    // No action required.
}